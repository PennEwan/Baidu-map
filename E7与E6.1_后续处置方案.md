# E7×E6.1 后续处置方案：E6.1 漏纳最小修复与定向验证

版本 v1.0，2026-09-15。依据 [E7与E6.1_道路重合度比较](E7与E6.1_道路重合度比较.md) 及以下已归档材料：

- [E7_冲突路段真实复核报告](backend/docs/E7_冲突路段真实复核报告.md)
- [E7_真实路线最短路实验报告](backend/docs/E7_真实路线最短路实验报告.md)
- [E7_真实路线最短路实验方案](backend/docs/E7_真实路线最短路实验方案.md)
- [E6.1_2.4公里真实运行报告](backend/docs/E6.1_2.4公里真实运行报告.md)
- [E6.1_2.4公里范围调整说明](backend/docs/E6.1_2.4公里范围调整说明.md)
- [E6.1_二维圈线与国定一社区运行计划](backend/docs/E6.1_二维圈线与国定一社区运行计划.md)

本方案只覆盖 **E6.1 漏纳最小修复线**。E7 改进（分段结构与分段耗时保存、道路身份、共享边权一致性、独立边界验证）不在本轮，另立方案。

## 0. 决策摘要

| 事项 | 决策 |
| --- | --- |
| 修复基座 | E6.2 候选以 2.4 km（±1200 m）运行为基座，对应 `guodingyi-boundary-surface-e61-2400`；原 ±1600 m 冻结结果只用于回放与对照，两范围不混用 |
| 修复范围 | 仅 E6.1 线：反证撤销 + 补测优先级 + 按 P1 证据再分配预算 |
| E7 角色 | 只给复核点排序；不得单独判定可达/不可达，不得把估计覆盖为真实标签 |
| 30 QPS | 纳入 P1 离线前置验证；未通过前 P3 保持 3 QPS |
| 真实预算 | P0/P1/P2 零请求；P3 额度在 P0–P2 完成后按分量数量单独申请，不预先写死 |
| 产物边界 | 不改 E6.1/E7 原始源码与 JSON；unknown 不自动填面；不扫系数、不自动补采 |

## 1. 已确认与未决事实（冻结口径）

### 1.1 已确认

1. **西南 38.399 m 冲突为 E6.1 漏纳反例。** 三个实际点 878/887/885 秒落在原暂定不可达面内；该面顶点 1218/1280/901 秒、内部检查 1490 秒、面积 49016.529 m²。三点距原不可达区边界约 5.997/15.286/7.041 m，不是共边计算误差。
2. **撤销路径有效。** 将 3 条实测反证加入 `connect_regions` 后，该整面不可达标签可撤销为 unknown（49016.529 m²）。这是应撤销断言的范围，**不是已证明漏掉的可达面积**；3 个点不足以证明整个 38.4 m 冲突路段可达。
3. **E7 严格同点裁决不足。** 6 个新实际点中仅 SW2 同时落在 E7 可达道路支持与 E6.1 不可达面内：E7 估计 893.636 秒，百度实测 887 秒。其余 5 点因离开旧路网 0.5 m 支持返回 unknown。
4. **E7 边界带（780—1020 秒）**：141 个不同实际终点中可估计 39 个（27.66%）；时间候选 MAE 18.39 秒、P95 32.96 秒、平均有符号误差 −15.54 秒（低估嫌疑）；70 个真实可达点未知；两个误纳点均为 908 秒。
5. **E6.1 2.4 km 运行现状**：800 次发送、817 面（151 暂定可达 / 108 暂定不可达 / 558 未知），未知面积 1.823283 km²（31.6542%），`unfinishedBoundary=true`、262 个 unfinished 包夹；预算结构为初始化 84 / 探索 79 / 边界补测 314 / 内部检查 323。

### 1.2 未决

1. **西侧 22.907 m 冲突未严格同点裁决。** 新实际点 903/911/920 秒支持 E6.1 不可达分类；E7 在原请求点估计 880.712/889.328/897.856 秒，但端点不同，不能写成同点误差，也不能宣布整条旧路段已裁决。
2. **5.704 km 未知缺口**（E7 估计可达但 E6.1 unknown）尚未逐段复核，不能自动填入可达面。
3. **30 QPS 未验证。** `RateGate`、串行锁、传输层固定 3 QPS 守卫未核对，不能只改环境变量就提速。

### 1.3 统计与精度限制

- 比较不是独立真值验证：两模型共享同一批 799 条真实采样。
- E7 边权是分摊估计，缺少道路 ID 与分段耗时；近邻平行轨迹未主观合并。
- 共边处对浮点共线敏感，对外只保留约 81% 的概括值，不宣称末位精度。
- 旧 93 点参考只作诊断/回归，不得用于选择权重、坐标或通过门槛。

## 2. P0 零请求：归档与差异清单冻结

### 2.1 归档

- 将 `temp/` 六份文档复制到 `backend/docs/`（本方案写作时已完成；原 `temp/` 保留）。
- E6.1/E7 原始源码与产物仍位于外部工作区（`D:/CodexOutputs/...`、`D:/CodexWorktrees/guodingyi-accuracy-v3`），不入仓、不改动。

### 2.2 冲突清单

- 从 `comparison-e61/overlay.geojson` 的 `outside` 分类拆出连通分量：西侧 22.907 m（未裁决）与西南 38.399 m（已裁决 + 撤销回放）。
- 每段记录：几何、长度、E7 端点估计时间、E6.1 状态、是否已被 6 点补测覆盖、剩余未裁决长度。
- 西侧段标记为 P3 的首个同点补测对象；西南段保留"撤销为 unknown"结论，不改为可达。

### 2.3 未知清单

- 将 5.704 km 未知缺口拆连通分量，逐段记录：E7 端点时间距 900 秒余量、与可达/未知/白区邻接、距最近采样与端点失效位置、对应 E6.1-2400 的 558 个未知面（按范围裁剪对齐）。
- 选点规则先冻结：近 900 秒 + 空间离散 + 邻接可达面优先；生成规则与点哈希写入 `protocol`，观察结果后不得改规则。

### 2.4 产物

- `discrepancy.json`：分量表、输入哈希、选点规则。
- `discrepancy.md`：中文说明与图。
- 已知答案合成检查（分量拆分、线长去重、范围裁剪）通过才算完成。
- 本轮零真实请求，不改 E6.1/E7 源码与产物。

## 3. P1 零请求：机制与前置审计

1. **整面反证普查。** 用原 799 条观测与 2.4 km 运行账本分别回放：统计"顶点与中心全同类"的面中，后来被观测、路线经过或边界证据反证的数量、位置与面积。
2. **撤销路径审计。** 在冻结代码上验证反证撤销的触发条件、不升可达、端点偏移与重复观测处理；以西南面 49016.529 m² 为基准回归。
3. **预算结构量化。** 对 2.4 km 运行的初始化 84 / 探索 79 / 边界 314 / 内部 323 逐项核对：远处同类内部检查实际改变分类或撤销标签的次数，作为再分配依据；不凭直觉调整。
4. **30 QPS 离线前置。** 在测试环境注入时钟检查 `RateGate`、串行锁与传输层固定 3 QPS 常量；覆盖到达节拍、429/冷却、取消、截止、重试加权。未通过前 P3 保持 3 QPS。
5. **产物**：`e61-face-audit.md` + 审计 JSON。零真实请求。

## 4. P2 零请求：E6.2 最小候选

1. **反证撤销优先**：任何与面标签矛盾的观测（含路线经过、边界点、冲突复核）立即把该面撤销为 unknown 并进入补测队列；撤销永不直接升为可达。
2. **补测优先级**：E7 候选位置 + unfinished 包夹 + 冲突道路；E7 只负责排序，不提供标签。
3. **预算再分配**：按 P1 第 3 项证据，把预算从低收益的远处同类内部检查移向边界包夹与冲突路段；总预算与阶段规则不放松。
4. **测试先行**：覆盖混合面不填充、反证撤销、面不升可达、包夹、预算归属、端点偏移、范围裁剪、取消/截止。随后跑合成与历史账本回放；旧 93 点只作诊断/回归。
5. **准入沿用既有门槛**：边界 FN 净降、FP 不增、无新增 >1020 秒误纳；几何支持、未知缺口、预算、取消、重试、确定性、端点保护全部通过。默认入口不切换，候选与报告单独交付。

## 5. P3 有限真实验证（需单独授权）

1. **预注册并冻结点集**：
   - 西侧 22.907 m 未裁决段同点补测（解决 E7 低估嫌疑）；
   - 5.704 km 未知缺口分层采样（近 900 秒 + 空间离散 + 邻接可达面）；
   - 不依赖 E7 估计的对照点，防止选择偏差；
   - 固定哨兵检测时间漂移。
2. **调用控制**：复用一次性账本、端点 ≤50 m、每观测最多 2 次、QPS 3 或经 P1 通过的 30、阶段截止 600 秒；权限/限流/配额/参数错误或凭据风险立即阻断后续发送。
3. **额度**：P0–P2 完成后按分量数量申请，独立封顶、不互借、不自动补采。
4. **产物**：`verify-results.json` + 中文报告，回填 P0 清单；逐点裁决 ≤900 / >900 / 无效端点。

## 6. P4 交付与边界

- E7 改进另立方案：分段结构与分段耗时保存、道路身份与分段匹配、共享边权一致性、独立边界验证、提升 27.66% 覆盖。
- 不改 E6.1/E7 原始源码、结果 JSON、账本；不在真实发送后改协议或点集。
- 不把 unknown 自动填面；不把 E7 估计当标签；不把两次运行范围混为一次；更新当前进度文档。

## 7. 输入依赖（须由 E7/E6.1 执行方提供）

| 阶段 | 路径（外部工作机） |
| --- | --- |
| P0 | `D:/CodexOutputs/guodingyi-route-graph-e7/comparison-e61/metrics.json`、`audit.json`、`overlay.geojson` |
| P1 | 同上目录的 `conflict-verification/protocol.json`、`face-replay.json`、`live/observations.json`、`audit.json`、`counterexample-replay.json` |
| P1 | `D:/CodexOutputs/guodingyi-route-graph-e7/results.json`、`time-predictions.json`、`unknown-support-diagnostic.json`、`time-reachable-roads.geojson` |
| P1 | `D:/CodexOutputs/guodingyi-boundary-surface-e61/run-01/observations.json`、`result.json`；`guodingyi-boundary-surface-e61-2400/run-01/ledger.json`、`observations.json`、`result.json`、`summary.json`、`brackets.json`、`frozen.json` |

在收到这些输入前，P0/P1 只能完成归档、规则冻结与本方案文件，不能产出差异清单或审计数字。

## 8. 停止条件

- 反证撤销路径不完整或会破坏一致性 → 停止候选。
- P2 离线准入不达标 → 不进 P3，不扫系数、不换候选重试。
- P3 西侧仍无法同点裁决，或出现限流/凭据/端点风险 → 阻断发送并出结论报告。
- 任何阶段发现需要改动 E7 才能继续 → 停下，另立 E7 方案，不在本线夹带。

当前状态：P0 归档已完成；差异清单与 P1/P2 等待第 7 节输入。真实调用为 0。
