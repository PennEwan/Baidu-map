# E6.1 国定一社区：2.4 公里范围真实重跑报告

2026-09-15 用户授权“重跑一下”后执行一次独立真实分析。结果已完成，质量 `partial`，停止原因为预算耗尽；本轮未修改算法、未追加任务、未提交或推送代码。

## 实际配置与版本

- 时间：北京时间 2026-09-15 22:32:40 至 22:39:16，算法耗时 395.64 秒。
- 中心：BD09LL `121.513926, 31.313077`，政立路 570 号附近。实际路线起点为 `121.51392519758, 31.313079085826`。
- 正方形总宽 2400 m，东西南北各 1200 m；面积 5.76 km²，关闭自动扩展。初始网格 400 m、最小网格 50 m，阈值 900 秒，随机种子 20260911。
- 总预算 800 次（包括失败、重试）；本轮冻结为 3 QPS、单并发、8 秒单次超时、每位置最多尝试 2 次、600 秒截止。
- 用户在运行中告知百度额度已为 **30 QPS**。本轮保持已经启动的 3 QPS 配置，没有中途改变或重启；30 QPS 尚未在本轮验证。下一轮需要同步检查 `RateGate`、串行锁、传输层中固定的 3 QPS 守卫，不能只改环境变量就认定提速完成。
- 工作区 `D:/CodexWorktrees/guodingyi-accuracy-v3`，分支 `codex/endpoint-integration-e5`，基础提交 `1277e76d92f7e1cc97500ffcb827ef80ae29b5d0`。包含未提交的研究实现，以 `run-01/frozen.json` 的 133 份源码哈希和依赖快照确定实际版本。
- Python 3.11.15；NumPy 2.4.6、Shapely 2.1.2、ContourPy 1.3.3、HTTPX 0.28.1、pytest 9.1.1，使用既有 D 盘环境。

## 调用与几何结果

800 次发送全部取得 HTTP 200、百度业务状态 0，无失败、无重试、无守卫阻断。全部路线起终点可核验，85 次终点偏移超过 50 m，按实际端点入库；726 个唯一实际终点，加显式起点零约束后共 727 个点。

实测任意一秒窗口最高 3 次，最大在途并发 1；最小发送间隔 0.400613 秒，最小响应后间隔 0.345820 秒。所有请求均位于新范围内，最大轴向距离 1200.0202 m，属于六位小数归一化的 0.1 m 容差内。

| 指标 | 原 3.2 km 运行 | 本轮 2.4 km 运行 |
| --- | ---: | ---: |
| 总调用 | 800 | 800 |
| 初始化调用（含重试） | 145 | 84 |
| 探索调用 | 79 | 79 |
| 边界补测调用 | 253 | 314 |
| 内部检查调用 | 323 | 323 |
| 暂定可达面积 | 0.944928 km² | 0.937345 km² |
| 可达分量 / 孔洞 | 9 / 4 | 8 / 1 |

初始化查询位置从 144 减至 84；旧轮额外 1 次是失败后的重试。边界预算增加 61 次，本轮具体为 242 次中点查询及 72 次偏移补偿。没有减少总预算，但把更多调用转向边界。

最终 817 个面：151 个暂定可达、108 个暂定不可达、558 个未知。未知面积 1.823283 km²，占新范围 31.6542%。圈线未接触范围边缘，`truncated=false`，但 `unfinishedBoundary=true`；保存 262 次单步包夹，均为 unfinished。不能把完整几何输出理解为全部边界已达到目标定位精度。

## 同范围比较与限制

将旧结果裁剪到相同的 ±1200 m 后，旧未知比例为 **35.9515%**，本轮为 **31.6542%**，下降约 4.30 个百分点。新旧可达面的交并比为 **87.6024%**，这是两次模型输出的重合度，**不是对独立真值的 IoU**。

此次没有独立社区参考集或现场步行核验，也没有重做 E7 冲突点的独立验证。请求集合发生变化且两次 API 查询时间不同，不能将全部差异归因于范围调整。孔洞、分量仍是模型结果，不保证对应真实物理结构。

缩小范围后，初始化中大于 1800 秒的位置从 70 降至 17，内部检查中大于 1800 秒的位置从 73 降至 19；但内部核验仍用去 323 次调用。此前发现的“同类顶点与中心不能保证整面同类”问题未在本轮修复。

## 验收与产物

- 联网前完整算法及后端测试 **829 passed，70.63 秒**；保留两条已有 Starlette/AnyIO 弃用警告。
- 实际预算、QPS、并发、范围、几何有效性审计全部通过。可达/不可达/未知区域无面积重叠，三者完整划分计算范围。
- 本轮训练观测未发现与输出整面分类矛盾的内部点；这仅是训练一致性，不是独立精度验收。
- 133 份冻结源码哈希在运行后不变。历史账本、结果、冻结配置及 HTML 哈希不变。
- 12 份 JSON/HTML/SVG 产物通过服务端 AK 精确值扫描。未输出密钥、请求 URL 或原始异常。
- PNG 已视觉复核，HTML 正常打开，实际采样点开关已验证并开启。页面使用业务坐标和本次 API 路线参考，未加载百度底图，不产生额外步行请求；设施统计尚未接入。

产物目录：`D:/CodexOutputs/guodingyi-boundary-surface-e61-2400/`。

| 文件 | 内容 |
| --- | --- |
| `full-final.xml` | 完整离线回归 |
| `run-01/frozen.json` | 版本、源码、依赖及配置 |
| `run-01/ledger.json` | 800 次调用与时序记录 |
| `run-01/observations.json`、`evidence.json`、`brackets.json` | 路线、实际端点及处理证据 |
| `run-01/result.json`、`summary.json` | 业务几何及摘要 |
| `run-01/audit.json`、`comparison.json`、`credential-scan.json` | 审计及同范围比较 |
| `run-01/guodingyi-e61-2400.html`、`.png`、`.svg` | 可缩放圈图及静态图 |

当前查看地址：<http://127.0.0.1:8767/guodingyi-e61-2400.html>。旧图仍在 8766 端口。

离线复核命令（PowerShell）：

```powershell
Set-Location D:/CodexWorktrees/guodingyi-accuracy-v3
$env:PYTHONPATH='D:/CodexWorktrees/guodingyi-accuracy-v3/backend;D:/CodexWorktrees/guodingyi-accuracy-v3/life-circle-algorithm/src'
$env:PYTHONUTF8='1'
$env:PYTHONDONTWRITEBYTECODE='1'
& D:/CodexCaches/baidu-map-algorithm-venv/Scripts/python.exe D:/CodexOutputs/guodingyi-boundary-surface-e61-2400/audit_run.py
& D:/CodexCaches/baidu-map-algorithm-venv/Scripts/python.exe -m http.server 8767 --bind 127.0.0.1 --directory D:/CodexOutputs/guodingyi-boundary-surface-e61-2400/run-01
```

本轮使用 `tools.endpoint_boundary_surface_live --execute-live` 执行，账本已耗尽并锁定，不能删除账本或重新获得本轮额度。

下一步建议：保持 2.4 km 范围，先离线验证 30 QPS 下的限速与并发调度，再优化远处同类区域内部检查，将预算进一步集中到边界包夹及路线冲突处；之后使用独立边界点验证效果。
