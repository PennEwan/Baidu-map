# 双算法并存说明

日期：2026-09-17。原“仅保留 Hybrid、退役纯百度”的方案已撤销；当前按产品要求同时维护两套算法。

- `/api/analyses`：纯百度自适应网格算法，保留原请求、任务、结果、设施与路线证据契约。
- `/api/v1/analysis/hybrid`：OSM＋百度 Hybrid v1.5，使用独立的 Hybrid 请求和结果契约。
- 两个管理器共享百度调用限流器，但任务、幂等 ID 和结果互相隔离。
- 前端保留 `analysis/ApiApp.tsx` 与 `hybrid/HybridApp.tsx`，由算法选择器挂载其中一个。
- 旧的精度报告仍是历史证据；自动测试只证明接口与流程可运行，不构成真实精度结论。

因此，不应再把 `/api/analyses` 注册成 Hybrid 别名，也不应删除纯百度页面、测试或契约。
