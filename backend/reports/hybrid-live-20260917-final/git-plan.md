# Git提交方案（尚未执行）

本轮真实链路运行成功，但完整精度验收为证据不足。可以提交“Hybrid单方案工程基线＋真实验收记录”，不应标为“精度90%已验收”或发布达标版本。

## 当前状态

- 分支：feat/osm-offline-baidu-test-report；HEAD：2b539b4f01b2a01645cf6f34eeb3202bf8b97fad。
- 预检：47项已跟踪修改、82项已跟踪删除、34组未跟踪条目；暂存区为空。未跟踪数包含目录，不是实际文件总数。
- 这些变化包含此前整理及历史删除，不全属于本次验证；提交前须逐项确认82项删除的归属。
- 当前候选文本扫描未发现本地服务端密钥，没有单文件超过10 MiB；这不是完整凭据审计。提交前再检查暂存内容及报告文件清单。

## 建议步骤

1. 保留当前工作区，备份已跟踪补丁及未跟踪源码/证据；新建工作分支（建议名chore/hybrid-only-validation），不重置工作区、不恢复历史删除。
2. 生成并审阅路径清单，重点复核历史报告、测试、脚本的82项删除。未确认的历史删除留在暂存区外，不混入本次功能提交。
3. **第一条提交：生产方案收敛。** 将Hybrid后端与OSM依赖、默认路由、前端Hybrid页面、客户端、契约、同步后的自动测试和必要配置文档放在同一原子提交中，防止中间提交出现前后端不兼容。新文件用明确路径加入，已跟踪修改按文件/补丁审阅。建议标题：`refactor: make OSM/Baidu Hybrid the only production analysis`。
4. **第二条提交：验收证据。** 加入本目录report.md、validation-map.png、metrics.json、cases.csv、audit.json、冻结结果、计划、账本及哈希记录；本次所有生成/核验证据一起保留，便于复算。审阅后再决定纳入旧百度2.0/2.1报告归档。建议标题：`test: record Hybrid live audit with insufficient precision evidence`。描述明确写92.31%容差、87.18%严格、78/100可判定、边界90.91%，不能只写92.31%。
5. **历史清理单独提交（可选）。** 只有确认历史文件删除清单后，才将其纳入独立归档/清理提交，不夹带未审阅删除。
6. 提交前检查暂存差异、空白错误、密钥和大文件；确认.env、虚拟环境、OSM图缓存/原始数据、浏览器输出、临时目录未进入暂存。现有.gitignore已覆盖这些本地文件，但backend/reports并未忽略，需要显式选取。
7. 对准备提交的版本运行后端回归、算法包回归、前端单元和构建、离线界面/联调；无需为Git提交重跑收费精度测试。核对测试与本次冻结源码一致。
8. 创建本地提交后检查每条提交的文件范围和最终状态；若需要推送，再核对远程目标并按用户后续要求推送/创建PR。PR写明“可运行、精度未验收”，不标记可发布达标。

## 审阅命令示例

以下仅为步骤示例，本次没有执行任何暂存、提交或分支切换：

```powershell
git switch -c chore/hybrid-only-validation
git status --short
git diff --name-status
git diff --check
# 按上述审核后的明确路径逐项暂存新文件和修改
git diff --cached --stat
git diff --cached --check
git diff --cached
# 确认每条提交范围后再执行相应 git commit
```

[本轮验证报告](report.md)
