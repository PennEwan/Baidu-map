# OSM＋百度验证：运行与90%精度核验报告

日期：2026-09-17。版本：Hybrid v1.5.0。固定起点 BD09LL (121.513925, 31.313079)。

**结论：链路能正常完成真实分析；本轮不能认定达到90%验收标准。** 可判定点的±15秒容差一致率为92.31%，但严格900秒一致率只有87.18%；有效可判定点78个不足80个，边界组容差90.91%也未达到95%门槛。程序结论为 `insufficient_evidence`（证据不足），不得改写为“已通过”。

## 1. 运行核验

通过当前生产应用的 `POST /api/v1/analysis/hybrid` 创建任务，轮询任务并读取结果，随后冻结圈面、生成账本和独立选点计划。任务 `7d0903ed-b620-4913-970f-5a5ae0c58723` 完成；算法版本 `hybrid-v1.5.0`。

- 生成400次，独立核验100次，合计500次；账本全部为completed、HTTP200、百度业务状态0，没有追加重跑或补点。
- 生成样本381个有效、19个端点偏移无效。HTTP调用成功不等于路线端点证据有效。
- OSM图、数据版本、覆盖边界、起点/计算范围覆盖、障碍层和风险层七项检查全部为真。
- 仍有未解析水线警告，readiness为degraded、quality为partial；诊断显示影响最终圈面的未解析水线数为0，所以既有工具允许独立核验。保留警告，未将其强制改成full。
- 圈面几何合法，包含起点，面积约1.186平方公里，未被计算范围截断。停止原因为预算耗尽，不能据此宣称边界已经完全收敛。
- 任务耗时391.0秒，其中准备阶段140.2秒（含障碍层加载64.3秒）、算法计算250.4秒。首次加载至最后核验响应约774.4秒，包含任务开始前的城市图加载。
- 旧工具generation-task.json中的cold_load_seconds只计TestClient启动，未计惰性城市图加载；不把该字段当作完整冷启动耗时。报告采用上述明确区间。

本轮验证了真实任务接口、成圈和独立路线核验；未新增真实设施检索或浏览器精度验收。上一轮同一工程整理的回归记录为后端352通过/1跳过、算法包96通过、前端156通过、浏览器5＋2通过，构建通过。本轮未重跑这些完整回归，也未改变生产源码；前后源码哈希已核对一致。

## 2. 预先固定的验收口径

15分钟=900秒；端点偏移阈值50米。先冻结圈面，再固定随机种子20260916选点，验证点与生成样本去重。分配为边界88、推断区5、其余内部5、远处圈外2。独立标签不回流修改圈面。

严格列按900秒判定；容差列仅豁免885—915秒之间的分类分歧。未知点不算通过，也不直接算失败，单独报告并受最低有效数约束。项目既定完整验收还要求边界容差至少95%。

| 门槛 | 本轮结果 | 判断 |
| --- | --- | --- |
| 总体容差一致率≥90% | 72/78=92.31% | 数值达到 |
| 严格900秒一致率≥90%（单列观察） | 68/78=87.18% | 未达到 |
| 可判定样本≥80 | 78/100 | 未达到 |
| 各适用分层≥80%可判定 | 边界66/88=75.00%，其余100% | 未达到 |
| 可达与不可达各≥20 | 可判定点中可达42、不可达36 | 达到 |
| 边界容差一致率≥95% | 60/66=90.91% | 未达到 |

## 3. 独立精度结果

| 分层 | 计划 | 可判定 | 严格900秒一致率 | ±15秒容差一致率 | 容差误纳 / 漏纳 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 边界两侧50米 | 88 | 66 | 84.85% | 90.91% | 3 / 3 |
| 内部推断区 | 5 | 5 | 100.00% | 100.00% | 0 / 0 |
| 其余内部 | 5 | 5 | 100.00% | 100.00% | 0 / 0 |
| 远处圈外 | 2 | 2 | 100.00% | 100.00% | 0 / 0 |

22个弃判点全部位于边界组：8个百度路线端点偏移超过50米；14个百度参考有效，但位于算法证据支持区外，算法保留未知。后者参考标签为13个不可达、1个可达。不能事后删掉这些点或把未知强行改为不可达来改善验收。

容差误纳3点、漏纳3点；另有4个严格分歧点在±15秒内被容差豁免。总体容差一致率的95% Wilson区间为84.22%—96.43%，不足以证明真实一致率稳定高于90%。100个计划点中确认容差一致的为72个；72/100是计划点确认比例，不等同于分类准确率。

补充诊断：若仅比较公开圈面的纳入/排除，不保留算法未知分类，在92个端点有效参考点上严格为81/92=88.04%，容差为86/92=93.48%。这不是预先固定的验收口径，仅便于理解圈面本身，不替代上述“证据不足”结论。

本轮为单地点、边界优先分层抽样；不代表全区域面积准确率、多地点泛化水平、整条边界的秒级误差保证或现场实走真值。

![生成圈面与独立核验分布](validation-map.png)

## 4. 建议

可以保留当前Hybrid作为可运行工程基线，但暂不标为“90%验收通过版本”。优先分析14个算法未知点的证据覆盖、8个端点偏移点，以及6个容差外错判点。生成策略若调整，应另开版本、重新冻结结果并使用新的独立参考集；本轮100次额度已用完，不续接补测凑门槛。

在完整门槛达到后，再增加不同路网/河道/封闭区域地点的独立验证。当前只记录问题与提交方案，不修正圈面或调整阈值。

## 5. 证据与完整性

- frozen-result.json：冻结公开结果；tasks/7d0903ed-b620-4913-970f-5a5ae0c58723/：生成计划、账本、诊断、结果。
- validation-plan.json、validation-started.json、validation-ledger.json、metrics.json：独立核验计划、一次性启动标记、原始证据和指标。
- audit.json：调用状态、端点/未知分类、几何及去重核对；cases.csv：逐点明细；git-preflight.json：源码指纹和提交预检。
- 圈面核心哈希：`30d87f89e06313df85776cb4a7d3556d36913d615accc8d3670940f2d6fe956d`。汇总工具已验证结果、生成账本、诊断和选点计划哈希；生成/核验坐标重合为0。
- 未修改算法、密钥或OSM数据；未执行git add、commit、push。

[Git提交方案](git-plan.md)
