# E6.1 国定一社区：2.4 公里范围调整

日期：2026-09-15。按用户最新要求，将后续 E6.1 真实实验入口的计算与请求范围缩小。修改完成时尚未运行新的真实分析，也未替换生产后端的 E5 默认入口。

后续状态：用户授权重跑后已完成一次 800 次真实分析，见 [2.4 公里真实运行报告](E6.1_2.4公里真实运行报告.md)。下文“尚未运行”及“本次新增真实调用 0 次”描述范围配置修改阶段，不能作为当前真实执行状态。

| 参数 | 历史运行 | 后续运行 |
| --- | --- | --- |
| 正方形总宽 | 3200 m | 2400 m |
| 起点到东、西、南、北边 | 1600 m | 1200 m |
| 面积 | 10.24 km² | 5.76 km² |
| 初始网格 | 400 m，8×8 格 | 400 m，6×6 格 |
| 初始化位置（含起点） | 145 | 85 |
| 初始化外部查询位置（不含重试） | 144 | 84 |

中心保持 BD09LL `121.513926, 31.313077`。900 秒阈值、50 m 最小网格、种子 20260911 保持原值。配置同时设置 `extent=max_extent=1200`、`expand=False`；初始化、探索、边界补测、内部检查与几何计算共用该范围。发送前的账本守卫拒绝范围外目标，只容许六位小数坐标归一化产生的 0.1 m 数值余量；API 实际端点仍按返回位置处理，不能将偏移点强行放回请求位置。

初始位置减少 60 个（41.7%），范围面积减少 43.75%。这不意味着总调用数必然减少：现有总预算上限仍为 800，节省的位置预算可被后续阶段使用。此次只缩小固定范围，尚未实现按方向逐步扩展的初始化策略，也未修复已发现的同类顶点及中心造成的整面漏纳。

## 产物与历史隔离

修改入口为 `backend/tools/endpoint_boundary_surface_live.py`。后续入口使用 `D:/CodexOutputs/guodingyi-boundary-surface-e61-2400/`；旧的 `D:/CodexOutputs/guodingyi-boundary-surface-e61/`、800 次账本、冻结配置和浏览器中的 `guodingyi-e61.html` 保留。新旧范围的结果不能混为同一次运行，也不能直接用范围缩小后的未知比例声称算法精度提高。

新真实运行仍要求显式 `--execute-live`、完整离线回归凭据和独立的一次性账本。本次修改不启动该入口，不复用或重置旧额度。

## 离线验证

新增测试覆盖初始化 84 个外部位置、四个方向越界请求在计数前被拒绝、归一化后的边界位置可发送，以及完整合成计算各阶段的请求及输出范围。修复前新增范围用例 5 项失败；修复后 E6、E6.1 和 E7 相关回归 **42 项通过，耗时 3.56 秒**。合成测试关闭 QPS 等待，真实入口仍保持 3 QPS。测试产物在上述新目录，`range-red.xml` 记录修复前失败，`range-green.xml` 记录回归结果。本次新增真实调用 0 次，未重新运行全仓库测试。

可复制命令（PowerShell，仓库根目录）：

```powershell
$env:PYTHONPATH='D:/CodexWorktrees/guodingyi-accuracy-v3/backend;D:/CodexWorktrees/guodingyi-accuracy-v3/life-circle-algorithm/src'
$env:PYTHONUTF8='1'
$env:PYTHONDONTWRITEBYTECODE='1'
& D:/CodexCaches/baidu-map-algorithm-venv/Scripts/python.exe -m pytest backend/tests/test_endpoint_boundary_surface.py backend/tests/test_endpoint_boundary.py backend/tests/test_route_graph_experiment.py -q -p no:cacheprovider --basetemp=D:/CodexOutputs/guodingyi-boundary-surface-e61-2400/tmp-rerun --junitxml=D:/CodexOutputs/guodingyi-boundary-surface-e61-2400/range-green.xml
```

下一步：确认下一轮真实调用预算后，用新范围生成独立圈图，检查边界是否碰到计算范围，比较初始化、边界补测和远处内部检查各自成本。范围外继续视为未评估，不能断言不存在可达区域。
